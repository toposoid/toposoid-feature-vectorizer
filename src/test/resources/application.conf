deployment = ${?DEPLOYMENT}

local{
  COMMON_NLP_JP_WEB_HOST = ${?COMMON_NLP_JP_WEB_HOST}
  COMMON_NLP_EN_WEB_HOST = ${?COMMON_NLP_EN_WEB_HOST}
  TOPOSOID_VECTORDB_ACCESSOR_HOST = ${?TOPOSOID_VECTORDB_ACCESSOR_HOST}
  TOPOSOID_VECTORDB_ACCESSOR_PORT = ${?TOPOSOID_VECTORDB_ACCESSOR_PORT}
}
